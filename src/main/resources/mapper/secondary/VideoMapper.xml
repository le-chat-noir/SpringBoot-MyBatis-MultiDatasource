<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="link.orangecat.multisource.persistence.dao.secondary.VideoDao">
    <select id="selectVideo" resultType="link.orangecat.multisource.persistence.entity.secondary.VideoEntity">
        select * from video where v_id = #{v_id}
    </select>
</mapper>